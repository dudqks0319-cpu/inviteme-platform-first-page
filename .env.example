# ============================================
# InviteMe Platform 환경변수 가이드
# ============================================
#
# 이 파일을 복사하여 .env.local 파일을 생성하세요:
# cp .env.example .env.local
#
# ⚠️  WARNING: 실제 키는 .env.local에만 입력하세요!
# ⚠️  .env.local 파일은 Git에 커밋되지 않습니다.
# ============================================

# --------------------------------------------
# Next.js 기본 설정
# --------------------------------------------
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --------------------------------------------
# Clerk 인증 (선택적 - 나중에 설정)
# --------------------------------------------
# Clerk 대시보드에서 발급: https://clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_replace_me
CLERK_SECRET_KEY=sk_test_replace_me

# Sign-in/Sign-up URL
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# --------------------------------------------
# Stripe 결제 (선택적 - 나중에 설정)
# --------------------------------------------
# Stripe 대시보드에서 발급: https://stripe.com
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
STRIPE_SECRET_KEY=sk_test_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me

BILLING_PLAN_ENV=dev

# --------------------------------------------
# 카카오 API (InviteMe 기능용)
# --------------------------------------------
# 카카오 개발자 사이트에서 발급: https://developers.kakao.com
# ⚠️  나중에 설정 가능 - 먼저 코드 테스트만 하려면 더미 값 사용
NEXT_PUBLIC_KAKAO_MAP_KEY=YOUR_KAKAO_MAP_KEY_HERE
# 공유 버튼은 APP_KEY를 우선, 없으면 MAP_KEY를 fallback으로 사용합니다.
NEXT_PUBLIC_KAKAO_APP_KEY=YOUR_KAKAO_APP_KEY_HERE

# --------------------------------------------
# Supabase 데이터베이스 (선택적 - 나중에 설정)
# --------------------------------------------
# Supabase 프로젝트에서 발급: https://supabase.com
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# --------------------------------------------
# 데이터베이스 (선택적 - 나중에 설정)
# --------------------------------------------
# PostgreSQL 연결 URL
# DATABASE_URL=postgresql://user:password@localhost:5432/inviteme
# DIRECT_URL=postgresql://user:password@localhost:5432/inviteme

# --------------------------------------------
# Sentry 에러 트래킹 (선택적)
# --------------------------------------------
# NEXT_PUBLIC_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
# SENTRY_DSN=your_sentry_dsn_here

# --------------------------------------------
# 개발용 로깅
# --------------------------------------------
# Logtail 또는 다른 로깅 서비스
# LOGTAIL_SOURCE_TOKEN=your_token_here

# ============================================
# 현재 필수 키 (우선순위순)
# ============================================
#
# 1. 기본 실행 (필수):
#    - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
#    - CLERK_SECRET_KEY
#    - NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
#    - STRIPE_SECRET_KEY
#    - STRIPE_WEBHOOK_SECRET
#    - NEXT_PUBLIC_CLERK_SIGN_IN_URL
#    - BILLING_PLAN_ENV
#
# 2. 초대장 생성 기능 (권장):
#    - NEXT_PUBLIC_KAKAO_MAP_KEY (지도 표시용)
#    - NEXT_PUBLIC_KAKAO_APP_KEY (카카오톡 공유용, 미설정 시 MAP_KEY fallback)
#
# 3. 데이터베이스 (선택):
#    - DATABASE_URL
#
# 4. 관측/로깅 (선택):
#    - NEXT_PUBLIC_SENTRY_DSN
#    - LOGTAIL_SOURCE_TOKEN
#
# ============================================

# ============================================
# 카카오 API 키 발급 방법
# ============================================
#
# 1. https://developers.kakao.com 접속
# 2. "내 애플리케이션" -> "애플리케이션 추가하기"
# 3. 앱 이름 입력 후 생성
# 4. "앱 키" 탭에서:
#    - JavaScript 키 -> NEXT_PUBLIC_KAKAO_MAP_KEY
#    - JavaScript 키 -> NEXT_PUBLIC_KAKAO_APP_KEY (동일 값)
# 5. "플랫폼" 탭에서:
#    - Web 플랫폼 추가
#    - 사이트 도메인 등록: http://localhost:3000
# 6. "동의항목" 탭에서:
#    - 카카오톡 메시지 전송 권한 활성화
#
# ============================================
